import 'package:flutter/material.dart';

class TexColumn extends StatelessWidget {
  const TexColumn({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}